# 6 可迭代物件（iterable）觀念

你需要知道（不一定會實作）：

- `list` / `tuple` / `dict` / `set` / `str` / `file` 都是 iterable
- iterator 只能走一次（`zip`, `filter`, `groupby`）

用途（對應第一章範例）：

- 解包 iterable
- `generator` / `zip` / `filter` / `compress`

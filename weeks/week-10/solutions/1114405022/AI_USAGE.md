# AI_USAGE

## 我問 AI 的問題

- Week 10 作業應該要有哪些檔案。
- `task1_csv_to_json.py` 和 `task2_json_to_xml.py` 的函式結構要怎麼安排。
- 測試應該如何涵蓋正常、邊界與反例。
- `@timeit` 裝飾器應該套在哪些函式上。
- Task 3 圖表應該怎麼產生與命名。

## 我採用的建議

- 使用獨立的 `timeit` 裝飾器包住 I/O 函式。
- 把 Task 1 的輸出整理成單一 summary dict。
- Task 3 用固定字典資料畫比較圖，避免依賴外部時間結果。

## 我拒絕的建議

- 不直接依賴課程原始 CSV 檔路徑硬寫死，因為本地環境不一定完整。
- 不把測試寫成只檢查單一路徑或單一案例，因為作業要求至少 10 個測試。

## 我修正過的錯誤

一開始我把 `default_input_path()` 的上層路徑算錯，導致程式會找不存在的位置。後來改成正確的父層級後，腳本才可以正常定位資料檔。
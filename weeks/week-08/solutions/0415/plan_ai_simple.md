# AI 教學簡單版本（含中文註解）

## 計劃一：最大值判斷

```python
# 計算一串數字的最大值
# 輸入：一行數字，以空白分隔
# 輸出：最大值
nums = list(map(int, input().split()))
print(max(nums))
```

---

## 計劃二：字串反轉

```python
# 將輸入字串反轉
# 輸入：一行字串
# 輸出：反轉後的字串
s = input()
print(s[::-1])
```
